server:
  port: 0

spring:
  application:
    name: picker-service
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    generate-ddl: true

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/picker
    username: root
    password: root

  redis:
    host: localhost
    port: 6379
