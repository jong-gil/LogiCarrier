server:
  port: 8085

spring:
  application:
    name: order-service
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    generate-ddl: true

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/robot
    username: root
    password: root

  redis:
    host: localhost
    port: 6379


logging:
  level:
    root: INFO

variable:
  line: 10

field:
  x: 10
  y: 10